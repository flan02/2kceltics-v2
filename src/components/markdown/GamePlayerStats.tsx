'use client'
import MarkdownRenderer from './MarkdownRenderer'
import BoxScoreMarkdown from '@/components/markdown/BoxScoreMarkdown.mdx'

type Props = {
  markdown: string
}

const GamePlayerStats = ({ markdown }: Props) => {

  return (
    <></>
  )
}

export default GamePlayerStats